- name: Deploy clouds.yaml
  template:
    src: clouds.yaml.j2
    dest: "{{ (zuul_work_dir + '/clouds.yaml') }}"
    mode: 0440

- name: debug
  debug:
    var: "{{ item }}"
  with_file:
    - "{{ (zuul_work_dir + '/clouds.yaml') }}"

